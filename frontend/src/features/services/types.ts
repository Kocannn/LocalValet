/**
 * Services Feature - Types
 * 
 * Feature-specific types for service management.
 * Re-export from shared types for feature isolation.
 */

export type { ServiceModule, ServiceStatus } from '@/types';
